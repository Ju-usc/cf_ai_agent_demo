/**
 * Shared constants for backend implementation and tests
 */

/**
 * Root prefix for all agent workspaces in R2
 * All agent files are stored under this root
 */
export const AGENT_WORKSPACE_ROOT = 'memory/';
